using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IgnoreLayerCollider : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
public static void IgnoreLayerCollision(int Enemies, int Ground, bool ignore);
    // Update is called once per frame
    void Update()
    {
        
    }
}
